<mah:MetroWindow x:Class="SmartPos.Views.KardexView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartPos.Views"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="Historial de Movimientos (Kardex)" 
        Height="500" Width="850" 
        WindowStartupLocation="CenterOwner"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Artículo:" FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="TxtNombreArticulo" Text="Nombre del Producto" FontSize="16" Margin="10,0,20,0"/>
            <TextBlock Text="Stock Actual:" FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="TxtStockActual" Text="0.00" FontSize="16" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="DgKardex" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  Background="Transparent">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding FechaTransaccion, StringFormat='dd/MM/yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoMovimiento}" Width="120"/>
                <DataGridTextColumn Header="Referencia" Binding="{Binding Referencia}" Width="120"/>
                <DataGridTextColumn Header="Costo unitario" Binding="{Binding CostoUnitario}" Width="120"/>

                <DataGridTemplateColumn Header="Cantidad" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CantidadMovimiento, StringFormat='N2'}" 
                                       FontWeight="Bold"
                                       HorizontalAlignment="Right">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EsEntrada}" Value="True">
                                                <Setter Property="Foreground" Value="#4CAF50"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding EsEntrada}" Value="False">
                                                <Setter Property="Foreground" Value="#F44336"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Saldo Final" Binding="{Binding CantidadNueva, StringFormat='N2'}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Usuario" Binding="{Binding ModificadoPor}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Cerrar" HorizontalAlignment="Right" 
                Width="100" Margin="0,15,0,0" IsCancel="True"
                Style="{StaticResource MahApps.Styles.Button.Flat}"/>
    </Grid>
</mah:MetroWindow>
