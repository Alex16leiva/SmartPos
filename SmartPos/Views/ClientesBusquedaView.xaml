<mah:MetroWindow x:Class="SmartPos.Views.ClientesBusquedaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartPos.Views"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="BUCADOR DE CLIENTES" Height="500" Width="850"
        WindowStartupLocation="CenterScreen"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" 
                 Text="{Binding clienteBusqueda, UpdateSourceTrigger=PropertyChanged}"
                 mah:TextBoxHelper.Watermark="Escriba para buscar por código o descripción..."
                 mah:TextBoxHelper.ClearTextButton="True"
                 FontSize="16" Margin="0,0,0,10"
                 mah:TextBoxHelper.AutoWatermark="True">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding SeleccionarClienteCommand}"/>
            </TextBox.InputBindings>
        </TextBox>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Clientes}" 
                  SelectedItem="{Binding ClienteSeleccionado, Mode=TwoWay}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Binding="{Binding Id}" Width="150"/>
                <DataGridTextColumn Header="NOMBRE" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="APELLIDO" Binding="{Binding Apellido, StringFormat='N2'}" Width="100"/>
                <DataGridTextColumn Header="TELEFONO" Binding="{Binding Telefono}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding ComandoPaginaAnteriorClientes}" Content="◀" Width="40" Style="{StaticResource MahApps.Styles.Button.Flat}"/>
                <TextBlock VerticalAlignment="Center" Margin="10,0">
                    <Run Text="Página "/>
                    <Run Text="{Binding PaginaActualClientes}" FontWeight="Bold"/>
                    <Run Text=" de "/>
                    <Run Text="{Binding TotalPaginasClientes}"/>
                </TextBlock>
                <Button Command="{Binding ComandoPaginaSiguienteClientes}" Content="▶" Width="40" Style="{StaticResource MahApps.Styles.Button.Flat}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="CANCELAR" Width="100" IsCancel="True" Margin="0,0,10,0"/>
                <Button Content="SELECCIONAR" Width="120" 
                        Style="{StaticResource MahApps.Styles.Button.Flat}"
                        Background="#28A745" Foreground="White"
                        Click="BtnSeleccionar_Click"/>
            </StackPanel>
        </Grid>

        <mah:ProgressRing Grid.Row="1" IsActive="{Binding IsBusy}"/>

    </Grid>
</mah:MetroWindow>
